import React from 'react';

export default function RegisterPage({ onNavigate }) {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <h1 className="text-2xl font-semibold">Register Page (placeholder)</h1>
      <p className="mt-2 text-sm text-slate-500">This page was replaced temporarily so the app can compile. I can restore the full implementation on request.</p>
    </div>
  );
}
